spring:
  application:
    name: sistema-veterinaria

  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/veterinaria_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:personalcorreoparaprueba@gmail.com}
    password: ${MAIL_PASSWORD:xntptnfgyhpckttm}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: "*"
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: none
    show-sql: true

file:
  upload-dir: ${UPLOAD_DIR:uploads}

admin:
  email: ${MAIL_USERNAME:personalcorreoparaprueba@gmail.com}

# Configuración externa de MiAPI Cloud (con valor por defecto integrado)
miapi:
  url: ${MIAPI_URL:https://miapi.cloud/apifact/invoice/create}
  secret: ${MIAPI_SECRET:miap-7ci-u47-raa}